export type RoomStatus = {
    id?: string
    name: string
    backgroundColor: string
    textColor: string
    keyWord: string
    border: number
}

export type RoomStatusForm = {
    name: string
    backgroundColor: string
    textColor: string
    keyWord: string
    border: number
}